machine:
  timezone:
    America/Denver
  node:
    version: 5.7.0
  services:
    - postgres
  environment:
    - JWT_SECRET: b0ldRs3cr3t
    - DB_HOST: 127.0.0.1
    - DB_USER: ubuntu
    - DB_NAME: circle_test
    - DB_PORT: 5432
test:
  pre:
    - npm run migrate
    - npm run seed
  override:
    - npm run test
