machine:
  timezone:
    America/Denver
  node:
    version: v5
  environment:
    PORT: 3000

dependencies:
  cache_directories:
    - node_modules
  post:
    - npm run build

test:
  override:
    - npm run test:api
